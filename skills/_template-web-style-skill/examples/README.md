Applied examples are created after this skill is used on fresh prompts and the result is visually confirmed.

Do not place source-site clones here.

Default validation example:

- If no target is specified, create `applied/portfolio/index.html`.
- Use a fictional but realistic personal portfolio.
- Include hero, navigation, project cases, capability/workflow section, experience or notes, contact CTA, responsive behavior, and one style-defining motif.

Reusable component snippets belong in `references/code-patterns/`, not in `examples/`.
