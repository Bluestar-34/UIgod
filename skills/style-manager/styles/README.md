# Managed Styles

These packages are read by `style-manager`.

They intentionally keep nested `SKILL.md` files so agents can recognize specific styles when nested skill scanning is enabled. `style-manager` provides the catalog and default selection behavior.
