# Skills

This directory is a flat visible skill namespace.

Default install:

- `uigod/`
- `style-manager/`

Managed styles live inside:

- `style-manager/styles/`

Managed styles keep nested `SKILL.md` files so they can be discovered by agents when nested scanning is enabled. Use `style-manager/` as the catalog and selection entry point.
